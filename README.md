# ⚡ ServerPlace

Маркетплейс сравнения облачных серверов VPS/VDS в России.

## О проекте

ServerPlace позволяет сравнить предложения от **140+ провайдеров** хостинга VPS/VDS в одном месте. Конфигуратор помогает подобрать оптимальный сервер по параметрам: vCPU, RAM, тип диска, канал, регион, виртуализация.

### Возможности

- 🔧 **Конфигуратор серверов** — динамический поиск с автообновлением результатов
- 📊 **Сравнение тарифов** — до 6 офферов в одной таблице
- ⭐ **Рейтинг провайдеров** — на основе отзывов пользователей
- 📝 **Отзывы** — верифицированные отзывы о провайдерах
- 📖 **Блог / FAQ** — статьи и ответы на частые вопросы
- 🔔 **Telegram уведомления** — заявки приходят в бот

### Технологии

- **Backend:** Node.js, Express, EJS
- **База данных:** SQLite (better-sqlite3)
- **Frontend:** Vanilla JS, CSS (светлая тема)
- **Деплой:** Nginx + systemd + Let's Encrypt SSL

## Установка

```bash
npm install
npm run seed   # Загрузка demo-данных
npm start      # Запуск на порту 4000
```

## Структура

```
├── server.js           # Точка входа
├── routes/
│   ├── pages.js        # Страницы (EJS)
│   ├── api.js          # REST API
│   └── admin.js        # Админ-панель
├── services/
│   ├── telegram.js     # Уведомления в Telegram
│   ├── scoring.js      # Скоринг офферов
│   └── email.js        # Email (legacy)
├── views/              # EJS-шаблоны
├── public/             # Статика (CSS, JS)
├── db/
│   ├── schema.sql      # Схема БД
│   ├── seed.js         # Сидирование
│   └── database.js     # Подключение
└── demo_data/          # JSON с демо-данными
```

## Автор

Сделано Денисом Смирновым.
